{
  "name": "nuxt-tailwind-admin-template",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "dotenv -e .env.development -- turbo run dev --parallel --no-daemon",
    "build": "dotenv -e .env.development -- turbo run build --no-daemon",
    "build:development": "dotenv -e .env.development -- turbo run build --no-daemon"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "commitizen": "4.3.0",
    "dotenv-cli": "7.3.0",
    "turbo": "1.12.2"
  },
  "packageManager": "pnpm@8.14.1"
}
